## Laubhaufen Workspace Notes

- This project is a Vite React TypeScript PWA.
- The frontend is designed to deploy on GitHub Pages.
- Free-tier Firebase is the intended sync backend for multi-user collaboration.
- If the repo name changes, update the Vite `base` path in `vite.config.ts`.
- Keep changes offline-first and browser-friendly.
